# 安全政策

## 报告漏洞

请不要为未修复的安全问题创建公开 issue。将复现步骤、影响范围、受影响版本和建议修复发送至 `jiangjingzhe2004@gmail.com`，标题以 `[AIOption Security]` 开头。

我们会尽力在 7 天内确认收到报告，并在完成评估后协调披露时间。

## 支持范围

安全修复优先覆盖默认分支的最新版本。旧提交、个人 fork、第三方部署和修改后的券商适配器不保证获得补丁。

## 操作者责任

- 不要把服务直接暴露到公网而不启用认证、TLS 和访问控制。
- 不要在仓库、issue、日志或截图中发布 API key、access token、订单 ID 或账户信息。
- 使用独立的模拟账户、最小权限凭证和秘密管理服务。
- 实盘前验证全局交易开关、用户权限、风控、订单监控和紧急全平。
- 发现凭证泄露时立即在供应商侧轮换；仅从 Git 历史删除并不能使凭证失效。

本政策不构成对实盘安全性、监管合规性或资金安全的保证。
